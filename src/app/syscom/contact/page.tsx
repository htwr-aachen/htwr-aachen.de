import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Noch noch mehr nichts",
  alternates: {
    canonical: "/syscom/contact",
  },
};

const About = () => (
  <div>
    <p>Hier bist du falsch</p>
  </div>
);

export default About;
