import type { Institutes } from "@/config/institutes";
import type { Subjects } from "@/config/subjects";

export const institute: Institutes = "cesti";
export const subjects: Subjects[] = ["itsec"];
