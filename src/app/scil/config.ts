import type { Institutes } from "@/config/institutes";
import { Subjects } from "@/config/subjects";

export const institute: Institutes = "scil";
export const subjects: Subjects[] = ["buk", "malo"];
