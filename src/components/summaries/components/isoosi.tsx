import type { FC } from "react";

const ISOOSI: FC = () => {
  return (
    <div className="my-2">
      <hr className="my-2 border-1 border-black"></hr>
      <span>
        Hier kommt irgendwann eine nice dynamische Graphik. Bin nicht so schnell
        mit programmieren und lernen :(
      </span>
    </div>
  );
};

export default ISOOSI;
